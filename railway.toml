[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["python312", "gcc"]

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port 8080"
healthcheckPath = "/health"
healthcheckTimeout = 60
```

**Passo 2 — Atualize o `requirements.txt`** com versões compatíveis com Python 3.12/3.13:
```
fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.10.0
slowapi==0.1.9
